debug: true #记录日志为debug级别

log:
  dir: D:\\tmp

application:
  message: hello word

server:
  servlet:
    jsp:
      init-parameters:
        development: true
spring:
  datasource:
    driver-class-name: org.h2.Driver
#    schema: classpath:db/schema-h2.sql
#    data: classpath:db/data-h2.sql
    schema: classpath:db/guns.sql
    url: jdbc:h2:mem:test
    username: root
    password: test